syntax = "proto3";

package service;

option go_package = "github.com/layer5io/service-mesh-performance/service;service";

enum ControllerStatus {
	RUNNING = 0;
	IDLE = 1;
}

message ControllerHealth {
    ControllerStatus status = 1;
}
